import React from 'react';
import Navbar from './Navbar';

const Header = () => {
    return (
        <div>
              <title>Navbar || Task Match</title>
            <Navbar></Navbar>
        </div>
    );
};

export default Header;